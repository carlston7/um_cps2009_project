import React from 'react';

const DummyTest = () => {
    return (
        <div>
            <h1>My code is so trash this doesn't print</h1>
            <h2>Test</h2>

            <p>User should still be logged in even when paging, the logout button shouldn't show bla bla</p>
        </div>
    );
};

export default DummyTest;
